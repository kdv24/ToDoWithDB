<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <title>Your search results!!!</title>
  </head>
  <body>
    <div class='container'>
      {% if results is not empty %}
        <h1>Your search results!!!</h1>

        {% for result in results %}
          <ul>
            <li>Your search term '{{ search_term }}' in category {{ result.getDescription }} is due on {{ result.getDueDate }}</li>
          </ul>
        {% endfor %}
      {% else %}
        <h1>No results! =(</h1>
      {% endif %}

      <a href='/'>Home!</a>
    </div>

  </body>
</html>
